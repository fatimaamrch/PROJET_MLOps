# Versions are pinned so that training (local) and serving (Docker / CI) use
# exactly the same libraries: a model pickled with scikit-learn X must be
# unpickled with scikit-learn X.

# Core ML
pandas==2.3.3
numpy==2.3.4
scikit-learn==1.7.2
joblib==1.5.2
cloudpickle==3.1.2

# Experiment tracking & registry
mlflow==3.16.1

# Config / plots
pyyaml==6.0.3
matplotlib==3.10.7

# Serving
fastapi==0.141.1
uvicorn[standard]==0.49.0
pydantic==2.13.3

# EDA notebook & report
seaborn==0.13.2
jupyterlab==4.4.10
ipykernel==7.1.0
nbconvert==7.16.6
nbformat==5.10.4

# Dev / quality
pytest==9.1.1
httpx==0.28.1
ruff==0.16.8
